import { Routes } from '@angular/router';

/**
 * Routes de l'application - application mono-page (SPA)
 * Toute la logique est dans le composant principal AppComponent
 */
export const routes: Routes = [];
